@model StudentAndCourse.Models.student

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Create</title>
</head>
<body>

<div>
    <h4>student</h4>
    <hr />
    <dl class="row">
    </dl>
</div>
<div>
    <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="text" placeholder="student Name" name="name" asp-for="name" class="btn btn-primary" />
            </div>
            <div class="form-group">
                <input type="text" placeholder="phone" name="phone" asp-for="phone" class="btn btn-primary" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    @Html.ActionLink("Edit", "Edit", new {  id = Model.id }) |
    <a asp-action="Index">Back to List</a>
</div>
</body>
</html>
